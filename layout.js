import './globals.css';

export const metadata = { title: 'KH Ozonator | Operations', description: 'Service operations prototype' };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
